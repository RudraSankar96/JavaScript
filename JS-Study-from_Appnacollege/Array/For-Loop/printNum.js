// Print all numbber using for loop
let marks=[60,50,70,80,90,];
for(i=0;i<marks.length;i++){
    console.log(marks[i]);  //here we can print 60,50,70,80,90
}

// Array Elements: Given an array let fruits = ["apple", "banana", "cherry"];, use a for loop to print each fruit in the array.

let fruits = ['apple','banana','cherry'];
for(let i=0;i<fruits.length;i++){
    console.log(fruits[i]);
}